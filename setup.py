from distutils.core import setup
from setuptools import find_packages

setup(name='za07ryma',
      version='0.1',
      author='DSSS',
      author_email='ahmed.sofan.fau@gmail.com',
      packages=find_packages(),
      install_requires=['numpy', 'Pillow', 'ipywidgets'])
